import { DashboardClient } from "@/components/dashboard/dashboard-client";

export default function DashboardPage() {
  // Page is now a simple shell, all logic is in DashboardClient
  return <DashboardClient />;
}
